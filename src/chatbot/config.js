import { createChatBotMessage } from "react-chatbot-kit";

const config = {
    botName: "Personal Bot",
    initialMessages: [createChatBotMessage(`Hello, How could I help you?`)]
  }
  
  export default config